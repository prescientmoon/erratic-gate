export interface SimulationRendererOptions {
    shadows: {
        enabled: boolean
        color: string
        lightHeight: number
        gateHeight: number
    }
    dnd: {
        rotation: number
    }
}
